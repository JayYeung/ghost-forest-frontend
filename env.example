# Google Maps API Key for Earth Engine Integration
# Replace this placeholder with your actual Google Maps API key
# Get your API key from: https://console.cloud.google.com/
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# Earth Engine OAuth Client ID
# Create this in Google Cloud Console under APIs & Services → OAuth consent screen → Web application
# Add http://localhost:3000 and your production domain to authorized origins
NEXT_PUBLIC_EE_OAUTH_CLIENT_ID=YOUR_EE_OAUTH_CLIENT_ID_HERE

# Note: Copy this file to .env.local and add your actual API keys
# The .env.local file is automatically ignored by git (see .gitignore) 